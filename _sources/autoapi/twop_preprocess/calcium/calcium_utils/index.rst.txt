twop_preprocess.calcium.calcium_utils
=====================================

.. py:module:: twop_preprocess.calcium.calcium_utils


Attributes
----------

.. autoapisummary::

   twop_preprocess.calcium.calcium_utils.print


Functions
---------

.. autoapisummary::

   twop_preprocess.calcium.calcium_utils.get_weights
   twop_preprocess.calcium.calcium_utils.rolling_percentile
   twop_preprocess.calcium.calcium_utils.correct_neuropil_ast
   twop_preprocess.calcium.calcium_utils.correct_neuropil_standard
   twop_preprocess.calcium.calcium_utils.dFF
   twop_preprocess.calcium.calcium_utils.calculate_and_save_dFF
   twop_preprocess.calcium.calcium_utils.estimate_offset
   twop_preprocess.calcium.calcium_utils.correct_offset
   twop_preprocess.calcium.calcium_utils.get_recording_frames


Module Contents
---------------

.. py:data:: print

.. py:function:: get_weights(ops)

   Get the weights for the suite2p detection.

   suite2p does not extract raw fluorescence traces, but rather uses a weighted sum of
   the raw fluorescence traces to detect ROIs. This function calculates the weights
   as in suite2p.detection.anatomical.select_rois (L161).
   For anatomical == 3 the weights are the intensity of each pixel relative to the
   1st and 99th percentile of the mean image + 0.1.

   :param ops: suite2p ops dictionary
   :type ops: dict

   :returns: weights for the suite2p detection
   :rtype: weights (numpy.ndarray)


.. py:function:: rolling_percentile(arr, window, percentile)

   Compute a rolling percentile over a 1D array.

   :param arr: 1D input array.
   :type arr: numpy.ndarray
   :param window: Window size in samples.
   :type window: int
   :param percentile: Percentile to compute (0-100).
   :type percentile: float

   :returns: The rolling percentile values (length = len(arr) - window + 1).
   :rtype: numpy.ndarray


.. py:function:: correct_neuropil_ast(dpath, Fr, Fn)

   Correct neuropil contamination using the Asymmetric Student's t-model (ASt).

   :param dpath: Path to the Suite2p plane folder.
   :type dpath: Path
   :param Fr: Raw fluorescence traces (n_rois x n_frames).
   :type Fr: numpy.ndarray
   :param Fn: Neuropil fluorescence traces (n_rois x n_frames).
   :type Fn: numpy.ndarray

   :returns: The ASt-corrected fluorescence traces (Fast).
   :rtype: np.ndarray


.. py:function:: correct_neuropil_standard(F, Fneu, neucoeff, save_path=None)

   Apply standard neuropil correction to fluorescence traces.

   Formula: F_corrected = F - neucoeff * (Fneu - median(Fneu)).

   :param F: Raw fluorescence traces (n_rois x n_frames).
   :type F: numpy.ndarray
   :param Fneu: Neuropil fluorescence traces (n_rois x n_frames).
   :type Fneu: numpy.ndarray
   :param neucoeff: Neuropil correction coefficient (usually 0.7).
   :type neucoeff: float
   :param save_path: Path to save the corrected trace as a
                     .npy file. If None, does not save. Default None.
   :type save_path: Path or str, optional

   :returns: The neuropil-corrected fluorescence traces.
   :rtype: np.ndarray


.. py:function:: dFF(f, n_components=2)

   Calculate ΔF/F using a Gaussian Mixture Model (GMM) to estimate baseline (F0).

   The baseline F0 is estimated as the mean of the lower component of a GMM
   fitted to the fluorescence distribution.

   :param f: Fluorescence traces (n_rois x n_frames).
   :type f: numpy.ndarray
   :param n_components: Number of GMM components. Default 2.
   :type n_components: int, optional

   :returns:

             (dff, f0)
                 - dff (np.ndarray): The calculated ΔF/F traces.
                 - f0 (np.ndarray): The estimated baseline values (n_rois x 1).
   :rtype: tuple


.. py:function:: calculate_and_save_dFF(dpath, F, filename_suffix, n_components=2)

   Calculate ΔF/F and save results to the Suite2p folder.

   :param dpath: Path to the Suite2p plane folder.
   :type dpath: Path
   :param F: Neuropil-corrected fluorescence traces.
   :type F: numpy.ndarray
   :param filename_suffix: Suffix for the output filenames (e.g., '_ast').
   :type filename_suffix: str
   :param n_components: Number of GMM components for baseline estimation.
                        Default 2.
   :type n_components: int, optional

   :returns:

             (dff, f0)
                 - dff (np.ndarray): The calculated ΔF/F traces.
                 - f0 (np.ndarray): The estimated baseline values.
   :rtype: tuple


.. py:function:: estimate_offset(datapath, n_components=3, save_path=None)

   Estimate the optical offset for a session by fitting a GMM to a raw TIFF frame.

   :param datapath: Path to the folder containing raw ScanImage TIFFs.
   :type datapath: str or Path
   :param n_components: Number of GMM components. Default 3.
   :type n_components: int, optional
   :param save_path: Path to save a diagnostic plot of the GMM fit.
   :type save_path: str or Path, optional

   :returns: The estimated offset (mean of the lowest GMM component).
   :rtype: float


.. py:function:: correct_offset(datapath, offsets, first_frames, last_frames)

   Subtract estimated offsets from concatenated fluorescence traces.

   :param datapath: Path to the .npy file containing concatenated traces.
   :type datapath: str or Path
   :param offsets: Array of offsets, one per recording.
   :type offsets: numpy.ndarray
   :param first_frames: Start frame indices for each recording.
   :type first_frames: numpy.ndarray
   :param last_frames: End frame indices for each recording.
   :type last_frames: numpy.ndarray

   :returns: The offset-corrected fluorescence traces.
   :rtype: np.ndarray


.. py:function:: get_recording_frames(suite2p_dataset)

   Get the frame boundaries for each recording in a concatenated Suite2p session.

   :param suite2p_dataset: Flexilims Dataset object for the Suite2p ROIs.
   :type suite2p_dataset: Dataset

   :returns:

             (first_frames, last_frames)
                 - first_frames (np.ndarray): Indices of the first frame for each recording.
                 - last_frames (np.ndarray): Indices of the last frame for each recording.
   :rtype: tuple


